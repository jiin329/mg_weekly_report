/**
 * App is the root Frontend component rendered inside the pywebview
 * Application_Window (not a browser tab).
 *
 * This is a Phase 1 skeleton only. The Chat_UI components (AppShell, RoomList,
 * MessageList, MessageBubble, InputArea, GenerateButton, ReportCard, etc.)
 * are implemented by the [FE] track tasks (see tasks.md section 3).
 */
export function App() {
  return (
    <main>
      <h1>주간보고 채팅</h1>
      <p>Frontend scaffold ready. Chat UI is implemented in later [FE] tasks.</p>
    </main>
  );
}
